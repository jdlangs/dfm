[project]
name = "dfm"
authors = [
    {name = "Josh Langsfeld", email = "josh.langsfeld@gmail.com"}
]
dependencies = [
    "click",
    "gitpython",
    "pyyaml",
    "rich",
]
version = "0.1.0"

[project.scripts]
dfm = "dfm:main"
